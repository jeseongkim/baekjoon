#include <string>
#include <vector>
#include <algorithm>

using namespace std;


int main(int argc, char const *argv[])
{
    int k, n;
    scanf("%d %d", &k, &n);
    std::vector<int> prime(k);
    for (int i = 0; i < k; ++i)
        scanf("%d",&prime[i]);
    vector<int> minmul;

    return 0;
}